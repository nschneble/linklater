#!/usr/bin/env bash
# flintest – run the project quality gate

set -uo pipefail

SCRIPT_DIR=$(cd -- "$(dirname -- "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)
ROOT_DIR=$(cd -- "$SCRIPT_DIR/.." && pwd)
cd "$ROOT_DIR" || exit

# shellcheck source=lib/cli.sh disable=SC1091
. "$SCRIPT_DIR/lib/cli.sh" 2>/dev/null ||
  { printf 'Cannot load %s/lib/cli.sh\n' "$SCRIPT_DIR" >&2; exit 70; }

# ── Arguments ───────────────────────────────────────────────────────────────────
# shellcheck disable=SC2317,SC2329 # Invoked by scan_help and unknown_flag.
usage() {
  cat <<'USAGE'
Usage: bin/flintest [options]

Runs the project quality gate in order:
  npm install
  node scripts/generate-email-palette.mjs
  npm run subset-fa --workspace @linklater/web
  npm run format
  npm run lint
  npm run typecheck
  npm run lint:migrations
  npm run test:cov
  npm run build

Examples:
  bin/flintest             Run the quality gate
  bin/flintest --tuffgal   Run the local visual regression check instead

Options:
  --tuffgal    Replace the default chain with a local visual regression run that
               self-diffs against the per-machine, git-ignored cache:
                 npm install
                 npx --no playwright install chromium
                 npm run tuffgal:setup
                 npx tuffgal run --local --manage-servers
               Opens the HTML report. When the run finds new or changed stories,
               it then prompts to approve them into the local cache. Committed
               baselines (tuffgal/baselines) are still CI-only; approving here
               only refreshes your local reference so the next run is clean.
  --no-input   Skip the approval prompt instead of asking.
  --no-pager   Print failure output in full instead of paging it.
  --no-color   Disable colored output.
  --version    Print the version and exit.
  -h, --help   Show this help.

See README.md for setup and usage notes.
USAGE
}

scan_help "$@"

RUN_TUFFGAL=false
NO_INPUT=false
NO_PAGER=false
USE_COLOR=true

KNOWN_FLAGS=(--tuffgal --no-input --no-pager --no-color --version --help -h)

for argument in "$@"; do
  case "$argument" in
    --tuffgal)
      RUN_TUFFGAL=true
      ;;
    --no-input)
      NO_INPUT=true
      ;;
    --no-pager)
      NO_PAGER=true
      ;;
    --no-color)
      USE_COLOR=false
      ;;
    --version)
      print_version
      ;;
    *)
      unknown_flag "$argument" "${KNOWN_FLAGS[@]}"
      ;;
  esac
done

# ── Terminal capability ─────────────────────────────────────────────────────────
probe_terminal

if [[ "${TERM:-dumb}" == dumb ]]; then
  TTY_AVAILABLE=false
fi

PAGER_FLAGS=(-FIRX)
PAGER_COMMAND=()
read -r -a PAGER_WORDS <<< "${PAGER:-}"
if [[ ${#PAGER_WORDS[@]} -gt 0 ]] && command -v "${PAGER_WORDS[0]}" > /dev/null 2>&1; then
  PAGER_COMMAND=("${PAGER_WORDS[@]}")
  if [[ ${#PAGER_COMMAND[@]} -eq 1 && ( "${PAGER_COMMAND[0]}" == less || "${PAGER_COMMAND[0]}" == */less ) ]]; then
    PAGER_COMMAND+=("${PAGER_FLAGS[@]}")
  fi
elif command -v less > /dev/null 2>&1; then
  PAGER_COMMAND=(less "${PAGER_FLAGS[@]}")
fi

# ── Colors & symbols ────────────────────────────────────────────────────────────
resolve_color "$USE_COLOR"

SPIN=(⠋ ⠙ ⠹ ⠸ ⠼ ⠴ ⠦ ⠧ ⠇ ⠏)
TICK=✓  CROSS=✗  CIRCLE=○

# ── Step table ──────────────────────────────────────────────────────────────────
if [[ "$RUN_TUFFGAL" == true ]]; then
  STEPS=(Install Playwright Database Test Report)
  # tuffgal local mode exits 0 on drift; a non-zero exit is a real failure
  STEP_COMMANDS=(
    'npm install'
    'npx --no playwright install chromium'
    'npm run tuffgal:setup'
    'npx tuffgal run --local --manage-servers'
    'open_report'
  )
else
  STEPS=(Install Generate Subset\ FA Format Lint Typecheck Lint\ Migrations Test Build)
  STEP_COMMANDS=(
    'npm install'
    'node scripts/generate-email-palette.mjs'
    'npm run subset-fa --workspace @linklater/web'
    'npm run format'
    'npm run lint'
    'npm run typecheck'
    'npm run lint:migrations'
    'npm run test:cov'
    'npm run build'
  )
fi
NS=${#STEPS[@]}

# Lines from status/cursor to step index i (0-based)
step_up() { echo $(( NS - $1 + 1 )); }

_spinner_pid=""

# ── Cursor helpers ──────────────────────────────────────────────────────────────
hide_cursor() { [[ "$TTY_AVAILABLE" == true ]] && printf '\033[?25l'; }
# shellcheck disable=SC2317,SC2329 # Invoked by _cleanup, which is registered as a trap.
show_cursor()  { [[ "$TTY_AVAILABLE" == true ]] && printf '\033[?25h'; }

# ── Cleanup on exit ─────────────────────────────────────────────────────────────
CURRENT_STEP="the run"
PAGING=false

# shellcheck disable=SC2317,SC2329 # Invoked by trap.
_cleanup() {
  stop_spinner
  show_cursor
  printf '\n'
}

trap _cleanup EXIT
# bash defers the signal until the pager exits; the verdict is already set
trap 'if [[ "$PAGING" != true ]]; then
        printf "\n  Stopping %s.\n" "$CURRENT_STEP" >&2
        stop_spinner
        show_cursor
        trap - EXIT INT TERM HUP QUIT
        exit 130
      fi' INT TERM HUP QUIT

# ── Draw initial UI ─────────────────────────────────────────────────────────────
draw_ui() {
  if [[ "$TTY_AVAILABLE" != true ]]; then
    printf 'Running Linklater checks…\n'
    return
  fi

  printf '\033[2J\033[H'
  printf '  %s%s╔═════════════════╗%s\n' "$BOLD" "$CYAN" "$RESET"
  printf '  %s%s║    Linklater    ║%s\n' "$BOLD" "$CYAN" "$RESET"
  printf '  %s%s╚═════════════════╝%s\n' "$BOLD" "$CYAN" "$RESET"
  printf '\n'
  for name in "${STEPS[@]}"; do
    printf "  ${DIM}${CIRCLE} %s${RESET}\n" "$name"
  done
  printf '\n'
  printf '  %sInitializing…%s' "$DIM" "$RESET"
}

# Write text onto a step line then return cursor to status line.
write_step() {
  local idx=$1; shift
  if [[ "$TTY_AVAILABLE" != true ]]; then
    printf '  %b\n' "$*"
    return
  fi

  local up; up=$(step_up "$idx")
  printf '\033[%dA\033[2K\r  %b\033[%dB\033[2K\r' "$up" "$*" "$up"
}

set_status() {
  if [[ "$TTY_AVAILABLE" != true ]]; then
    printf '%b\n' "$*"
    return
  fi

  printf '\033[2K\r  %b' "$*"
}

mark_done() { write_step "$1" "${GREEN}${TICK} ${STEPS[$1]}${RESET}"; }
mark_fail() { write_step "$1" "${RED}${CROSS} ${STEPS[$1]}${RESET}"; }

# ── Background spinner ──────────────────────────────────────────────────────────
start_spinner() {
  local idx=$1
  if [[ "$TTY_AVAILABLE" != true ]]; then
    printf '  %s…\n' "${STEPS[$idx]}"
    return
  fi

  local up name
  up=$(step_up "$idx")
  name="${STEPS[$idx]}"
  (
    local i=0
    while true; do
      local frame="${SPIN[$((i % ${#SPIN[@]}))]}"
      printf '\033[%dA\033[2K\r  %s%s %s%s\033[%dB\033[2K\r' \
        "$up" "$TTY_YELLOW" "$frame" "$name" "$TTY_RESET" "$up" > /dev/tty
      sleep 0.08
      i=$(( i + 1 ))
    done
  ) &
  _spinner_pid=$!
}

stop_spinner() {
  [[ "$TTY_AVAILABLE" == true ]] || return

  if [[ -n "$_spinner_pid" ]]; then
    kill "$_spinner_pid" 2>/dev/null || true
    wait "$_spinner_pid" 2>/dev/null || true
    _spinner_pid=""
  fi
}

# ── Step runner ─────────────────────────────────────────────────────────────────
# run_step <idx> <command> [args…]
# Returns 0 on success, 1 on failure (output stored in STEP_OUTPUT).
STEP_OUTPUT=""
run_step() {
  local idx=$1; shift
  CURRENT_STEP="the ${STEPS[$idx]} step"
  start_spinner "$idx"
  local output rc=0
  output=$("$@" 2>&1) || rc=$?
  stop_spinner
  CURRENT_STEP="the run"
  STEP_OUTPUT="$output"
  if [[ $rc -eq 0 ]]; then
    mark_done "$idx"
    return 0
  else
    mark_fail "$idx"
    return 1
  fi
}

dump_failure() {
  local verdict=$1 body=$2
  local rows body_lines
  rows=$(stty size 2>/dev/null < /dev/tty | cut -d ' ' -f 1)
  body_lines=$(printf '%s\n' "$body" | wc -l)
  if [[ "$NO_PAGER" != true && "$TTY_AVAILABLE" == true && ${rows:-0} -gt 0 ]] \
    && [[ $body_lines -gt $rows && ${#PAGER_COMMAND[@]} -gt 0 ]]; then
    PAGING=true
    printf '%s\n' "$body" | sed 's/^/  /' | "${PAGER_COMMAND[@]}"
    PAGING=false
  else
    printf '%s\n' "$body" | sed 's/^/  /' >&2
  fi
  printf '\n  %s%s%s%s\n' "$ERR_RED" "$ERR_BOLD" "$verdict" "$ERR_RESET" >&2
}

# ── Visual regression summary ───────────────────────────────────────────────────
# Mirrors the "Parse harness outcome" step in tuffgal-action: pulls the totals
# out of results.json so the final status line can show passed/new/changed/failed
# even when the harness exited 0. Empty string when no results.json was
# written (e.g. tuffgal crashed before producing one).
tuffgal_summary() {
  local results="$ROOT_DIR/tuffgal/report/results.json"
  if [[ ! -f "$results" ]]; then
    return
  fi
  # shellcheck disable=SC2016 # ${} below are JS template literals, not shell
  node -e '
    const path = process.argv[1];
    const raw = require("fs").readFileSync(path, "utf8");
    const totals = JSON.parse(raw).totals ?? {};
    const passed = totals.passed ?? 0;
    const newish = totals.new ?? 0;
    const changed = totals.changed ?? 0;
    const failed = totals.failed ?? 0;
    const stories = totals.stories ?? 0;
    process.stdout.write(`${passed}/${stories} passed, ${newish} new, ${changed} changed, ${failed} failed`);
  ' "$results" 2>/dev/null
}

# Echo "<new> <changed>" from the last run, or nothing when no results.json.
tuffgal_drift_counts() {
  local results="$ROOT_DIR/tuffgal/report/results.json"
  if [[ ! -f "$results" ]]; then
    return
  fi
  # shellcheck disable=SC2016 # ${} below are JS template literals, not shell
  node -e '
    const path = process.argv[1];
    const totals = JSON.parse(require("fs").readFileSync(path, "utf8")).totals ?? {};
    process.stdout.write(`${totals.new ?? 0} ${totals.changed ?? 0}`);
  ' "$results" 2>/dev/null
}

# Open the HTML report in the default browser (macOS `open`). No-op without a TTY.
# shellcheck disable=SC2317,SC2329 # Invoked indirectly as the Report step's command.
open_report() {
  local report="$ROOT_DIR/tuffgal/report/index.html"
  if [[ "$TTY_AVAILABLE" != true || ! -f "$report" ]]; then
    return
  fi
  open "$report" >/dev/null 2>&1 || true
}

# ── Main ────────────────────────────────────────────────────────────────────────
hide_cursor
draw_ui

FAILED_STEP=""
FAILED_OUTPUT=""

for (( index = 0; index < NS; index++ )); do
  read -r -a step_command <<< "${STEP_COMMANDS[$index]}"
  if ! run_step "$index" "${step_command[@]}"; then
    FAILED_STEP="${STEPS[$index]}"
    FAILED_OUTPUT="$STEP_OUTPUT"
    break
  fi
done

if [[ "$RUN_TUFFGAL" == true ]]; then
  TUFFGAL_SUMMARY=$(tuffgal_summary)

  if [[ -n "$FAILED_STEP" ]]; then
    if [[ -n "$TUFFGAL_SUMMARY" ]]; then
      set_status "${RED}${BOLD}${FAILED_STEP} failed. Output below.${RESET} ${DIM}${TUFFGAL_SUMMARY}${RESET}"
    else
      set_status "${RED}${BOLD}${FAILED_STEP} failed. Output below.${RESET}"
    fi
    [[ "$TTY_AVAILABLE" == true ]] && printf '\n'
    printf '\n'
    dump_failure "${FAILED_STEP} failed." "$FAILED_OUTPUT"
    exit 1
  fi

  read -r TUFFGAL_NEW TUFFGAL_CHANGED <<< "$(tuffgal_drift_counts)"
  TUFFGAL_NEW=${TUFFGAL_NEW:-0}
  TUFFGAL_CHANGED=${TUFFGAL_CHANGED:-0}

  if [[ -n "$TUFFGAL_SUMMARY" ]]; then
    set_status "${GREEN}${BOLD}Visual regression complete${RESET}\n  ${DIM}${TUFFGAL_SUMMARY}${RESET}"
  else
    set_status "${GREEN}${BOLD}Visual regression complete${RESET}"
  fi

  if [[ "$NO_INPUT" != true && "$TERMINAL_REACHABLE" == true && $(( TUFFGAL_NEW + TUFFGAL_CHANGED )) -gt 0 ]]; then
    show_cursor
    printf '\n\n  %sApprove %s new + %s changed into the local cache?%s [y/N] ' \
      "$TTY_BOLD" "$TUFFGAL_NEW" "$TUFFGAL_CHANGED" "$TTY_RESET" > /dev/tty
    APPROVE_REPLY=""
    read -r APPROVE_REPLY < /dev/tty || true
    if [[ "$APPROVE_REPLY" =~ ^[Yy]([Ee][Ss])?$ ]]; then
      printf '\n'
      if APPROVE_OUTPUT=$(npx tuffgal approve 2>&1); then
        printf '  %s%s Local cache refreshed. The next run will be clean.%s\n' "$GREEN" "$TICK" "$RESET"
      else
        printf '  %s%s tuffgal approve failed. Output below.%s\n\n' "$ERR_RED" "$CROSS" "$ERR_RESET" >&2
        dump_failure "tuffgal approve failed." "$APPROVE_OUTPUT"
        exit 1
      fi
    else
      printf '\n  %sLeft as-is. Re-run to re-check, or approve later with npm run tuffgal:approve.%s\n' \
        "$TTY_DIM" "$TTY_RESET" > /dev/tty
    fi
  else
    [[ "$TTY_AVAILABLE" == true ]] && printf '\n'
  fi
  exit 0
fi

if [[ -z "$FAILED_STEP" ]]; then
  set_status "${GREEN}${BOLD}All checks passed.${RESET}"
  [[ "$TTY_AVAILABLE" == true ]] && printf '\n'
  exit 0
else
  set_status "${RED}${BOLD}${FAILED_STEP} failed. Output below.${RESET}"
  [[ "$TTY_AVAILABLE" == true ]] && printf '\n'
  printf '\n'
  dump_failure "${FAILED_STEP} failed." "$FAILED_OUTPUT"
  exit 1
fi
