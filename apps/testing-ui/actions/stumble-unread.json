{
  "action": "stumble-unread",
  "steps": [
    { "kind": "navigate", "path": "/unread" },
    {
      "kind": "click",
      "hint": { "role": "button", "text": "Stumble!" }
    },
    {
      "kind": "waitFor",
      "hint": {
        "selector": "#links-list a[aria-label*='Test Link 2']"
      }
    },
    { "kind": "wait", "ms": 600 }
  ],
  "expect": {
    "anyOf": [{ "selector": "#links-list a[aria-label*='Test Link 2']" }]
  },
  "mask": [{ "selector": "#suggestion-callout-title" }],
  "diff": { "maxDiffRatio": 0.02 }
}
