VITE_API_BASE_URL="https://localhost:3000"

# Set to "true" to show the Google SSO button (requires API-side Google vars)
VITE_GOOGLE_SSO_ENABLED="false"

# Set to "true" to show the Apple SSO button (requires API-side Apple vars)
VITE_APPLE_SSO_ENABLED="false"
