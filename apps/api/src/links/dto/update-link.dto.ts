/**
 * Request body for PATCH /links/:id.
 *
 * // TODO: No user-editable fields are defined yet. The endpoint exists to
 * keep the REST surface complete for future additions (e.g., custom title,
 * tags). Until fields are added, PATCH is effectively a no-op that returns
 * the unchanged link.
 */
export class UpdateLinkDto {}
