#!/usr/bin/env bash
# stands in for node while the bin/ scripts are under test

set -uo pipefail

: "${BIN_TEST_SHIM_DIR:?}" "${BIN_TEST_CAPTURE:?}"

# shellcheck source=record.sh disable=SC1091
. "$BIN_TEST_SHIM_DIR/record.sh"

record_invocation node "$@"
