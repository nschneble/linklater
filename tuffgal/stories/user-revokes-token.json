{
  "story": "As a logged-in user, I can revoke an API token — and if revocation fails on the server, I see an error.",
  "needs": ["logged-in"],
  "actions": [
    { "action": "intercept-one-api-token" },
    { "action": "intercept-token-revoke-ok" },
    { "action": "revoke-token" },
    { "action": "intercept-one-api-token" },
    { "action": "intercept-token-revoke-500" },
    { "action": "revoke-token-and-expect-error" }
  ]
}
